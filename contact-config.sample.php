<?php

declare(strict_types=1);

// Copy this file to contact-config.php and fill in real values.
// contact-config.php is gitignored so the app password never reaches the repo.

// Generate an App Password at https://myaccount.google.com/apppasswords
// (requires 2-Step Verification enabled on the Google account).
const SMTP_USERNAME = 'kareem.mahmoud.abd.elhannan@gmail.com';
const SMTP_PASSWORD = 'REPLACE_WITH_16_CHAR_APP_PASSWORD';
